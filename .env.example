# Copy to .env and fill in. .env is gitignored — never commit it.
#
# Supabase project: loteria-analyzer  (ref zgcivxpkgpcrrmrxnafw, us-east-1)
#
# Get this from the Supabase dashboard:
#   Project -> Connect -> Session pooler  (NOT "Transaction pooler" — COPY needs a
#   session connection), then substitute your database password.
#
# If you have never set the database password for this project, set or reset it at
#   Project Settings -> Database -> Database password -> Reset database password
#
# Session pooler URIs look like:
#   postgresql://postgres.zgcivxpkgpcrrmrxnafw:YOUR-PASSWORD@aws-0-us-east-1.pooler.supabase.com:5432/postgres
#
# A direct connection also works if your network has IPv6:
#   postgresql://postgres:YOUR-PASSWORD@db.zgcivxpkgpcrrmrxnafw.supabase.co:5432/postgres

DATABASE_URL=
